export const processAggregatedData = (aggregatedJson) => {
  // Assuming aggregatedJson already has the desired structure { artist_counts: {}, style_counts: {} }
  // If any transformation is needed in the future, it can be added here.
  return aggregatedJson;
};